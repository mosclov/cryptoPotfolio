<%= form_for(@crypto) do |f| %>
  <% if @crypto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crypto.errors.count, "error") %> prohibited this crypto from being saved:</h2>

      <ul>
      <% @crypto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :qty %><br>
    <%= f.text_area :qty %>
  </div>
  <div class="field">
    <%= f.label "Location" %><br>
    <%= f.select :exchange, ["", "Binance", "Bittrex", "Coinbase Alex", "Coinbase Fede", "EtherDelta", "HitBtc", "Kucoin", "Ledger", "Other"] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
